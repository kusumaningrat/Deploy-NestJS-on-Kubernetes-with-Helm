apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookshelf.fullname" . }}-test-connection"
  labels:
    {{- include "bookshelf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookshelf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
